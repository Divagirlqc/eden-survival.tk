:root {
  --red: #ff2a2a;
  --dark: #0b0b0b;
  --dark2: #121212;
  --text: #eaeaea;
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: 'Segoe UI', sans-serif;
}

body {
  background: radial-gradient(circle at top, #2a0000, var(--dark));
  color: var(--text);
  min-height: 100vh;
}

/* Watermark Chaos */
.chaos-watermark {
  position: fixed;
  inset: 0;
  background: url("../assets/logo.png") center/420px no-repeat;
  opacity: 0.04;
  pointer-events: none;
  animation: pulse 8s infinite ease-in-out;
}

@keyframes pulse {
  0%,100% { opacity: 0.03; transform: scale(1); }
  50% { opacity: 0.06; transform: scale(1.05); }
}

header {
  text-align: center;
  padding: 60px 20px;
}

.logo {
  width: 140px;
  filter: drop-shadow(0 0 25px red);
}

.subtitle {
  color: var(--red);
  margin-top: 10px;
  font-weight: bold;
}

nav {
  display: flex;
  justify-content: center;
  gap: 25px;
  background: var(--dark2);
  padding: 15px;
}

nav a {
  color: var(--text);
  text-decoration: none;
  font-weight: bold;
}

nav a:hover {
  color: var(--red);
}

.container {
  padding: 60px 20px;
  text-align: center;
}

.cards {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 25px;
  margin-top: 40px;
}

.card {
  background: #160000;
  border: 1px solid #400;
  padding: 25px;
  border-radius: 12px;
  box-shadow: 0 0 20px rgba(255,0,0,0.15);
}

footer {
  text-align: center;
  padding: 20px;
  background: var(--dark2);
  font-size: 14px;
}
